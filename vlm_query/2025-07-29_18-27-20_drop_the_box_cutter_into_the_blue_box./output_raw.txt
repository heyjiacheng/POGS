```python
# The task is "Drop the box cutter into the blue box."
# From the image, keypoints 0, 1, and 2 are on the box cutter, and keypoint 4 is inside the blue box.
# This task involves 2 stages:
# 1. Grasp the box cutter
# 2. Move and drop the box cutter into the blue box

num_stages = 2

### stage 1 sub-goal constraints (grasp box cutter)
def stage1_subgoal_constraint1(end_effector, keypoints):
    """Align the end-effector with the handle of the box cutter (keypoint 1)."""
    target_point = keypoints[1]
    cost = np.linalg.norm(end_effector - target_point)
    return cost

### stage 1 path constraints
# No path constraints needed for grasping stage

### stage 2 sub-goal constraints (drop box cutter into blue box)
def stage2_subgoal_constraint1(end_effector, keypoints):
    """The end-effector is 10cm above the center of the blue box (keypoint 4)."""
    offset = np.array([0, 0, 0.10])
    offsetted_point = keypoints[4] + offset
    cost = np.linalg.norm(end_effector - offsetted_point)
    return cost

### stage 2 path constraints
def stage2_path_constraint1(end_effector, keypoints):
    """The robot must still be grasping the box cutter handle (keypoint 1)."""
    return get_grasping_cost_by_keypoint_idx(1)

grasp_keypoints = [1, -1]
release_keypoints = [-1, 1]
```